diff --git a/crawl-ref/source/describe.cc b/crawl-ref/source/describe.cc
index 0a381e125d..61fdd56a14 100644
--- a/crawl-ref/source/describe.cc
+++ b/crawl-ref/source/describe.cc
@@ -267,7 +267,15 @@ static string _format_prop_desc(string prop_name, string prop_desc)
 #endif
 
     const int name_len = strwidth(prop_name);
-    linebreak_string(prop_desc, 80 - name_len);
+    // Word wrap (Android): wrap at the panel width so the ui-layer wrap cap
+    // never re-folds these lines -- its folds don't carry the hanging indent.
+    extern int android_prose_wrap_cols;
+    extern bool android_reflow_prop_desc;
+    const int wrap_width = android_prose_wrap_cols > 0
+                           && android_reflow_prop_desc
+        ? max(20, android_prose_wrap_cols - name_len)
+        : 80 - name_len;
+    linebreak_string(prop_desc, wrap_width);
     string padding = string(name_len, ' ');
     string justified_desc = replace_all(prop_desc, "\n", "\n" + padding);
 
@@ -1384,11 +1392,17 @@ static string _skill_target_desc(skill_type skill, int scaled_target,
 static void _append_skill_target_desc(string &description, skill_type skill,
                                         int scaled_target)
 {
+    // Word wrap (Android): the 4-space indent hanging-indents every wrapped
+    // continuation line, so drop it when wrap is active.
+    extern int android_prose_wrap_cols;
+    extern bool android_reflow_weapon_stats;
+    const string pad = android_prose_wrap_cols > 0
+                       && android_reflow_weapon_stats ? "\n" : "\n    ";
     if (!you.has_mutation(MUT_DISTRIBUTED_TRAINING))
-        description += "\n    " + _skill_target_desc(skill, scaled_target, 100);
+        description += pad + _skill_target_desc(skill, scaled_target, 100);
     if (you.training[skill] > 0 && you.training[skill] < 100)
     {
-        description += "\n    " + _skill_target_desc(skill, scaled_target,
+        description += pad + _skill_target_desc(skill, scaled_target,
                                                     you.training[skill]);
     }
 }
@@ -1406,7 +1420,13 @@ static string _desc_attack_delay(const item_def &item)
 
     const int cur_delay = you.attack_delay_with(&dummy).expected();
 
-    return make_stringf("\n    Current attack delay: %.1f.", (float)cur_delay / 10);
+    // Word wrap (Android): drop the hanging indent (part of the skill block).
+    extern int android_prose_wrap_cols;
+    extern bool android_reflow_weapon_stats;
+    const char *pad = android_prose_wrap_cols > 0
+                      && android_reflow_weapon_stats ? "\n" : "\n    ";
+    return make_stringf("%sCurrent attack delay: %.1f.", pad,
+                        (float)cur_delay / 10);
 }
 
 static string _describe_missile_dmg_brand(const item_def &item)
@@ -1536,9 +1556,16 @@ static void _append_skill_needed(string &description, const item_def &item,
     const bool useful = !is_useless_item(item) && crawl_state.need_save;
     if (useful)
     {
-        description += "\n";
-        if (indent)
+        extern int android_prose_wrap_cols;
+        extern bool android_reflow_weapon_stats;
+        // Weapon/thrown callers (indent=true) get a spacer row above
+        // "Your skill:"; bauble/talisman callers do not.
+        description += indent ? "\n\n" : "\n";
+        if (indent && !(android_prose_wrap_cols > 0
+                        && android_reflow_weapon_stats))
+        {
             description += "    ";
+        }
         description += _your_skill_desc(skill, can_set_target, target_skill,
                                         std::move(skill_padding));
     }
@@ -1552,6 +1579,13 @@ static void _append_weapon_stats(string &description, const item_def &item)
     const int base_dam = property(item, PWPN_DAMAGE);
     const int mindelay_skill = _item_training_target(item);
 
+    // Word wrap (Android): stack the base stats vertically so the padded
+    // one-liner doesn't break at the wrap width.
+    extern int android_prose_wrap_cols;
+    extern bool android_reflow_weapon_stats;
+    const char *stat_sep = android_prose_wrap_cols > 0
+                           && android_reflow_weapon_stats ? "\n" : "  ";
+
     if (item.base_type == OBJ_STAVES
         && item.is_identified()
         && staff_skill(static_cast<stave_type>(item.sub_type)) != SK_NONE
@@ -1567,16 +1601,20 @@ static void _append_weapon_stats(string &description, const item_def &item)
     {
         const char *inf = Options.char_set == CSET_ASCII ? "inf" : "\u221e"; //"∞"
         description += make_stringf(
-            "Base accuracy: %s  Base damage: %s  ",
+            "Base accuracy: %s%sBase damage: %s%s",
             inf,
-            inf);
+            stat_sep,
+            inf,
+            stat_sep);
     }
     else
     {
         description += make_stringf(
-            "Base accuracy: %+d  Base damage: %d  ",
+            "Base accuracy: %+d%sBase damage: %d%s",
             property(item, PWPN_HIT),
-            base_dam);
+            stat_sep,
+            base_dam,
+            stat_sep);
     }
 
     description += make_stringf(
@@ -2162,11 +2200,18 @@ static string _describe_ammo(const item_def &item)
         const int throw_delay = (10 + dam / 2);
         const int target_skill = _item_training_target(item);
 
+        // Word wrap (Android): stack the padded stat line vertically.
+        extern int android_prose_wrap_cols;
+        extern bool android_reflow_missile_stats;
+        const char *stat_sep = android_prose_wrap_cols > 0
+                               && android_reflow_missile_stats ? "\n" : "  ";
+
         description += make_stringf(
-            "\n\nBase damage: %d  Base attack delay: %.1f"
+            "\n\nBase damage: %d%sBase attack delay: %.1f"
             "\nThis projectile's minimum attack delay (%.1f) "
                 "is reached at skill level %d.",
             dam,
+            stat_sep,
             (float) throw_delay / 10,
             (float) FASTEST_PLAYER_THROWING_SPEED / 10,
             target_skill / 10
@@ -2281,13 +2326,20 @@ static string _describe_armour(const item_def &item, bool verbose, bool monster)
         }
         if (!monster)
             description += "\n\n";
+        // Word wrap (Android): stack the padded stat line vertically.
+        extern int android_prose_wrap_cols;
+        extern bool android_reflow_armour_stats;
+        const bool stacked = android_prose_wrap_cols > 0
+                             && android_reflow_armour_stats;
         if (is_shield(item))
         {
             description += "Base shield rating: "
                         + to_string(property(item, PARM_AC));
-            description += "     Encumbrance rating: "
+            description += (stacked ? "\nEncumbrance rating: "
+                                    : "     Encumbrance rating: ")
                         + to_string(-property(item, PARM_EVASION) / 10);
-            description += "     Max blocks/turn: "
+            description += (stacked ? "\nMax blocks/turn: "
+                                    : "     Max blocks/turn: ")
                         + to_string(shield_block_limit(item));
             if (is_unrandom_artefact(item, UNRAND_WARLOCK_MIRROR))
                 description += _warlock_mirror_reflect_desc();
@@ -2299,14 +2351,16 @@ static string _describe_armour(const item_def &item, bool verbose, bool monster)
                         + to_string(property(item, PARM_AC));
             if (get_armour_slot(item) == SLOT_BODY_ARMOUR)
             {
-                description += "       Encumbrance rating: "
+                description += (stacked ? "\nEncumbrance rating: "
+                                        : "       Encumbrance rating: ")
                             + to_string(-evp / 10);
             }
             // Bardings reduce evasion by a fixed amount, and don't have any of
             // the other effects of encumbrance.
             else if (evp)
             {
-                description += "       Evasion: "
+                description += (stacked ? "\nEvasion: "
+                                        : "       Evasion: ")
                             + to_string(evp / 30);
             }
         }
@@ -4063,6 +4117,9 @@ command_type describe_item_popup(const item_def &item,
         footer_text += formatted_string(_actions_desc(actions));
         auto footer = make_shared<Text>();
         footer->set_text(footer_text);
+        // Word wrap (Android): mirror the body Text so the cyan action list
+        // folds at the panel width (ui.cc.patch cap) instead of overflowing.
+        footer->set_wrap_text(true);
         footer->set_margin_for_crt(1, 0, 0, 0);
         footer->set_margin_for_sdl(20, 0, 0, 0);
         vbox->add_child(std::move(footer));
@@ -4210,12 +4267,20 @@ void inscribe_item(item_def &item)
  */
 static string _player_spell_stats(const spell_type spell)
 {
+    // Word wrap (Android): stack Level/School/Fail vertically instead of
+    // padding them into one 80-col line that breaks at the wrap width.
+    extern int android_prose_wrap_cols;
+    extern bool android_reflow_spell_stats;
+    const bool stacked = android_prose_wrap_cols > 0
+                         && android_reflow_spell_stats;
+
     string description;
     description += make_stringf("\nLevel: %d", spell_difficulty(spell));
 
     const string schools = spell_schools_string(spell);
     description +=
-        make_stringf("        School%s: %s",
+        make_stringf("%sSchool%s: %s",
+                     stacked ? "\n" : "        ",
                      schools.find("/") != string::npos ? "s" : "",
                      schools.c_str());
 
@@ -4239,7 +4304,8 @@ static string _player_spell_stats(const spell_type spell)
     }
     else
         failure = failure_rate_to_string(raw_spell_fail(spell));
-    description += make_stringf("        Fail: %s", failure.c_str());
+    description += make_stringf("%sFail: %s", stacked ? "\n" : "        ",
+                                failure.c_str());
 
     const string damage_string = spell_damage_string(spell);
     const string max_dam_string = spell_max_damage_string(spell);
@@ -5580,6 +5646,24 @@ static void _attacks_table_row_throwing(const monster_info &mi,
 static void _build_table_of_attacks(mon_attack_desc_info &di,
                                     ostringstream &result)
 {
+    // Word wrap (Android): the 3-column table (attack | max damage | bonus)
+    // overflows the narrow panel, so a long bonus folds back to column 0 under
+    // "Attacks". When wrap is on, shorten the damage header to "Max Dmg" for
+    // room and hang-indent the bonus so continuations align under "Bonus".
+    extern int android_prose_wrap_cols;
+    extern bool android_reflow_attack_bonus;
+    const bool reflow = android_prose_wrap_cols > 0
+                        && android_reflow_attack_bonus && di.has_any_flavour;
+    const char *dmg_header = "Max Damage";
+    if (reflow)
+    {
+        dmg_header = "Max Dmg";
+        size_t dmg_w = strlen(dmg_header);
+        for (const string &d : di.damage_descriptions)
+            dmg_w = max(dmg_w, (size_t) strwidth(d));
+        di.damage_width = dmg_w;
+    }
+
     // Hopefully enough width for every possibility
     di.damage_width    = min(di.damage_width, (size_t) 31);
     di.bonus_width     = min(di.bonus_width, 69 - di.damage_width);
@@ -5596,24 +5680,42 @@ static void _build_table_of_attacks(mon_attack_desc_info &di,
     // First, the table header
     result << padded_str(di.plural ? "Attacks" : "Attack",
                          di.attk_desc_width + 2)
-           << padded_str("Max Damage", di.damage_width + 2);
+           << padded_str(dmg_header, di.damage_width + 2);
     if (di.has_any_flavour)
     {
-        result << padded_str(di.flavour_without_dam ? "Bonus"
-                                                    : "After Damaging Hits",
-                             di.bonus_width);
+        const string bonus_header = di.flavour_without_dam
+                                    ? "Bonus" : "After Damaging Hits";
+        // Bonus is the last column; skip trailing pad under reflow so the
+        // header row doesn't overshoot the panel and fold.
+        result << (reflow ? bonus_header
+                          : padded_str(bonus_header, di.bonus_width));
     }
     result << "\n";
 
     // Table body
+    const int bonus_col = di.attk_desc_width + 2 + di.damage_width + 2;
     for (unsigned int i = 0; i < di.attack_descriptions.size(); ++i)
     {
         result << chop_string(di.attack_descriptions[i], di.attk_desc_width)
                << "  "
                << chop_string(di.damage_descriptions[i], di.damage_width)
-               << "  "
-               << chop_string(di.bonus_descriptions[i], di.bonus_width)
-               << "\n";
+               << "  ";
+        if (reflow)
+        {
+            string bonus = di.bonus_descriptions[i];
+            if (!bonus.empty())
+            {
+                const int avail = max(8, android_prose_wrap_cols - bonus_col);
+                linebreak_string(bonus, avail);
+                bonus = replace_all(bonus, "\n", "\n" + string(bonus_col, ' '));
+            }
+            result << bonus << "\n";
+        }
+        else
+        {
+            result << chop_string(di.bonus_descriptions[i], di.bonus_width)
+                   << "\n";
+        }
     }
 }
 
@@ -7333,6 +7435,44 @@ static string _format_data_label(int value, bool show_sign, bool is_percent,
                                  red ? "</red>" : "");
 }
 
+#ifndef USE_TILE_LOCAL
+// Android word wrap (libandroid.cc): > 0 when the narrow-display word-wrap
+// mode is active. The talisman-table column labels are wide enough to force
+// wrapping on that layout, so they are shortened only in that mode; the stock
+// wide layout keeps the full upstream labels.
+extern int android_prose_wrap_cols;
+#endif
+
+static const char *_ww_label(const char *full, const char *shortened)
+{
+#ifndef USE_TILE_LOCAL
+    if (android_prose_wrap_cols > 0)
+        return shortened;
+#else
+    (void)shortened;
+#endif
+    return full;
+}
+
+// special_dice_name comes from dat/forms/*.yaml (kept at upstream length);
+// shorten the wide ones only in word-wrap mode. Table-only string, so this is
+// the single place it is displayed.
+static string _ww_special_dice_name(const string &full)
+{
+#ifndef USE_TILE_LOCAL
+    if (android_prose_wrap_cols > 0)
+    {
+        if (full == "Watery Grave Dmg")
+            return "Grave Dmg";
+        if (full == "Frigid Wall Dmg")
+            return "Frigid Dmg";
+        if (full == "Blinkbolt Dmg")
+            return "Blinkbolt";
+    }
+#endif
+    return full;
+}
+
 static void _maybe_note_armour_modifier(vector<vector<string>>& items,
                                         const Form& form,
                                         const int skill[3])
@@ -7353,7 +7493,7 @@ static void _maybe_note_armour_modifier(vector<vector<string>>& items,
         change[i] = base_ac * mult[i] / 100;
 
     vector<string> labels;
-    labels.push_back("Body Armour AC");
+    labels.push_back(_ww_label("Body Armour AC", "Body AC"));
 
     for (int i = 0; i < 3; ++i)
     {
@@ -7403,7 +7543,7 @@ static void _maybe_note_airstrike_damage(vector<vector<string>>& items,
     }
 
     vector<string> labels;
-    labels.push_back("Airstrike Dmg");
+    labels.push_back(_ww_label("Airstrike Dmg", "Airstrike"));
     labels.push_back(make_stringf("(%d-%d)d%d", data[0][0].num, data[0][1].num, data[0][0].size));
     labels.push_back(make_stringf("(%d-%d)d%d", data[1][0].num, data[1][1].num, data[1][0].size));
     labels.push_back(make_stringf("(%d-%d)d%d", data[2][0].num, data[2][1].num, data[2][0].size));
@@ -7492,23 +7632,23 @@ static string _describe_talisman_form(transformation form_type)
     items.push_back({"Skill", to_string(skill[0]), to_string(skill[1]), cur_skill});
 
     _maybe_populate_form_table(items, bind(&Form::mult_hp, form, 100, true, placeholders::_1), "HP", skill, -100, true, true, 1);
-    _maybe_populate_form_table(items, bind(&Form::get_base_unarmed_damage, form, false, placeholders::_1), "UC Base Dmg", skill, -3);
+    _maybe_populate_form_table(items, bind(&Form::get_base_unarmed_damage, form, false, placeholders::_1), _ww_label("UC Base Dmg", "UC Dmg"), skill, -3);
     _maybe_populate_form_table(items, bind(&Form::get_ac_bonus, form, placeholders::_1), "AC", skill, 0, false, true, 100, 1);
     _maybe_populate_form_table(items, bind(&Form::ev_bonus, form, placeholders::_1), "EV", skill, 0, false, true, 100, 1);
     _maybe_populate_form_table(items, bind(&Form::max_mp_bonus, form, placeholders::_1), "MP", skill);
     _maybe_populate_form_table(items, bind(&Form::slay_bonus, form, false, placeholders::_1), "Slay", skill);
     _maybe_populate_form_table(items, bind(&Form::regen_bonus, form, placeholders::_1), "Regen", skill, 0, false, true, 100, 2);
-    _maybe_populate_form_table(items, bind(&Form::mp_regen_bonus, form, placeholders::_1), "MP Regen", skill, 0, false, true, 100, 2);
-    _maybe_populate_form_table(items, bind(&Form::get_web_chance, form, placeholders::_1), "Ensnare Chance", skill, 0, true, false);
+    _maybe_populate_form_table(items, bind(&Form::mp_regen_bonus, form, placeholders::_1), _ww_label("MP Regen", "RegenMP"), skill, 0, false, true, 100, 2);
+    _maybe_populate_form_table(items, bind(&Form::get_web_chance, form, placeholders::_1), _ww_label("Ensnare Chance", "Ensnare %"), skill, 0, true, false);
     _maybe_note_armour_modifier(items, *form, skill);
 
-    _maybe_note_form_dice(items, bind(&Form::get_special_damage, form, false, placeholders::_1), form->special_dice_name, skill,
+    _maybe_note_form_dice(items, bind(&Form::get_special_damage, form, false, placeholders::_1), _ww_special_dice_name(form->special_dice_name), skill,
                           form_type == transformation::dragon && !species::is_draconian(you.species) ? " (x2)" : "");
 
     if (form_type == transformation::maw)
     {
-        _maybe_populate_form_table(items, bind(&Form::get_aux_damage, form, false, placeholders::_1), "Bite Dmg", skill, 0, false, false);
-        _maybe_populate_form_table(items, bind(&Form::get_effect_size, form, placeholders::_1), "Devour Regen", skill, 0, false, true, 100, 2);
+        _maybe_populate_form_table(items, bind(&Form::get_aux_damage, form, false, placeholders::_1), _ww_label("Bite Dmg", "Bite"), skill, 0, false, false);
+        _maybe_populate_form_table(items, bind(&Form::get_effect_size, form, placeholders::_1), _ww_label("Devour Regen", "Devour Reg"), skill, 0, false, true, 100, 2);
     }
     if (form_type == transformation::blade)
     {
@@ -7516,27 +7656,27 @@ static string _describe_talisman_form(transformation form_type)
         _maybe_populate_form_table(items, bind(&Form::get_effect_size, form, placeholders::_1), "Parry SH", skill, 0, false, false);
     }
     if (form_type == transformation::hive)
-        _maybe_populate_form_table(items, bind(&Form::get_effect_size, form, placeholders::_1), "# of Bees", skill, 0, false, false, 10, 1);
+        _maybe_populate_form_table(items, bind(&Form::get_effect_size, form, placeholders::_1), _ww_label("# of Bees", "Bees"), skill, 0, false, false, 10, 1);
     if (form_type == transformation::sphinx)
         _maybe_note_airstrike_damage(items, skill);
     if (form_type == transformation::werewolf)
     {
         _maybe_populate_form_table(items, bind(&Form::get_werefury_kill_bonus, form, placeholders::_1), "+Slay/Kill", skill, 0, false, false, 10, 1);
-        _maybe_populate_form_table(items, bind(&Form::get_takedown_multiplier, form, placeholders::_1), "Takedown Dmg", skill, 0, true, true);
+        _maybe_populate_form_table(items, bind(&Form::get_takedown_multiplier, form, placeholders::_1), _ww_label("Takedown Dmg", "Takedown"), skill, 0, true, true);
         _maybe_populate_form_table(items, bind(&Form::get_howl_power, form, placeholders::_1), "Howl Power", skill, 0, false, false);
     }
     if (form_type == transformation::walking_scroll)
-        _maybe_populate_form_table(items, bind(walking_scroll_skill_bonus, 10, placeholders::_1), "Spell Skill Boost", skill, 0, false, true, 10, 1);
+        _maybe_populate_form_table(items, bind(walking_scroll_skill_bonus, 10, placeholders::_1), _ww_label("Spell Skill Boost", "Spell Skill"), skill, 0, false, true, 10, 1);
     if (form_type == transformation::fortress_crab)
-        _maybe_populate_form_table(items, bind(&Form::get_effect_size, form, placeholders::_1), "Rust Breath Size", skill, 0, false, false);
+        _maybe_populate_form_table(items, bind(&Form::get_effect_size, form, placeholders::_1), _ww_label("Rust Breath Size", "Breath Size"), skill, 0, false, false);
     if (form_type == transformation::medusa)
     {
-        _maybe_populate_form_table(items, bind(&Form::get_effect_size, form, placeholders::_1), "Tendril targets", skill, 0, false, false, 10, 1);
-        _maybe_populate_form_table(items, bind(&Form::get_effect_chance, form, placeholders::_1), "Petrify chance", skill, 0, true, false);
+        _maybe_populate_form_table(items, bind(&Form::get_effect_size, form, placeholders::_1), _ww_label("Tendril targets", "Tendrils"), skill, 0, false, false, 10, 1);
+        _maybe_populate_form_table(items, bind(&Form::get_effect_chance, form, placeholders::_1), _ww_label("Petrify chance", "Petrify %"), skill, 0, true, false);
     }
     if (form_type == transformation::vampire)
     {
-        _maybe_populate_form_table(items, bind(&Form::get_effect_size, form, placeholders::_1), "Bat Swarm Recharge", skill, 0, true, false);
+        _maybe_populate_form_table(items, bind(&Form::get_effect_size, form, placeholders::_1), _ww_label("Bat Swarm Recharge", "Bat Recharge"), skill, 0, true, false);
         _maybe_populate_form_table(items, bind(&Form::get_effect_chance, form, placeholders::_1), "Daze Power", skill, 0, false, false);
     }
 
@@ -7569,11 +7709,22 @@ static string _describe_talisman_form(transformation form_type)
     // Actually output the table
     ostringstream description;
 
-    description << string(60, '_') << "\n\n";
+    // Word wrap (Android): the fixed 60-wide section rules and the column-
+    // wide table underline both overshoot the panel wrap width and get
+    // re-folded, doubling up. Cap them to the wrap width when wrap is on.
+    extern int android_prose_wrap_cols;
+    extern bool android_reflow_talisman_dividers;
+    const bool wrap_div = android_prose_wrap_cols > 0
+                          && android_reflow_talisman_dividers;
+    const int solid_div = wrap_div ? min(60, android_prose_wrap_cols) : 60;
+
+    description << string(solid_div, '_') << "\n\n";
 
     vector<formatted_string> lines = cols.formatted_lines();
     description << lines[0].to_colour_string() << "\n";
-    description << string(column_width.back(), '-') << "\n";
+    description << string(wrap_div
+                          ? min(column_width.back(), android_prose_wrap_cols)
+                          : column_width.back(), '-') << "\n";
     description << lines[1].to_colour_string() << "\n";
     description << lines[2].to_colour_string() << "\n";
     description << "<white>" << lines[3].to_colour_string() << "<lightgrey>\n";
@@ -7582,7 +7733,13 @@ static string _describe_talisman_form(transformation form_type)
         description << "\nClass: " << uppercase_first(holiness_description(form->holiness));
 
     // Now add various one-off bits of (generally non-scaling) data after that
-    TablePrinter pr(4, 80);
+    // Word wrap (Android): the 4-col grid pads each cell to 20 and folds mid-
+    // cell on the narrow panel. Stack one stat per row instead when wrap is on.
+    extern bool android_reflow_talisman_stats;
+    const bool stack_stats = android_prose_wrap_cols > 0
+                             && android_reflow_talisman_stats;
+    TablePrinter pr(stack_stats ? 1 : 4,
+                    stack_stats ? android_prose_wrap_cols : 80);
     pr.AddRow();
 
     if (form->size != SIZE_CHARACTER)
@@ -7677,7 +7834,7 @@ static string _describe_talisman_form(transformation form_type)
                     << "\n";
     }
 
-    description << string(60, '_') << "\n";
+    description << string(solid_div, '_') << "\n";
 
     return description.str();
 }
