diff --git a/crawl-ref/source/hiscores.cc b/crawl-ref/source/hiscores.cc
index e2e4409a74..c0318fd798 100644
--- a/crawl-ref/source/hiscores.cc
+++ b/crawl-ref/source/hiscores.cc
@@ -335,14 +335,14 @@ string hiscores_print_list(int display_count, int format, int newest_entry, int&
     return ret;
 }
 
+// Android: hands the morgue file path to the Java side, which opens it in
+// the native scrolling viewer (whole file, real vscroll) instead of the
+// 48-row terminal scroller, whose bitmap overflows the screen above the
+// keyboard. Defined in libandroid.cc.
+extern "C" void android_show_character_file(const char *path);
+
 static void _show_morgue(scorefile_entry& se)
 {
-    int flags = FS_PREWRAPPED_TEXT;
-    formatted_scroller morgue_file(flags);
-
-    morgue_file.set_tag("morgue");
-    morgue_file.set_more();
-
     string morgue_base = morgue_name(se.get_name(), se.get_death_time());
     string morgue_path = catpath(morgue_directory(),
                             strip_filename_unsafe_chars(morgue_base) + ".txt");
@@ -351,33 +351,8 @@ static void _show_morgue(scorefile_entry& se)
     if (!morgue) // TODO: add an error message
         return;
 
-    char buf[200];
-    string morgue_text = "";
-
-    while (fgets(buf, sizeof buf, morgue) != nullptr)
-    {
-        string line = string(buf);
-        size_t newline_pos = line.find_last_of('\n');
-        if (newline_pos != string::npos)
-            line.erase(newline_pos);
-        morgue_text += "<w>" + replace_all(line, "<", "<<") + "</w>" + '\n';
-    }
-
     lk_close(morgue);
-
-    column_composer cols(2, 40);
-    cols.add_formatted(
-            0,
-            morgue_text,
-            true);
-
-    vector<formatted_string> blines = cols.formatted_lines();
-
-    unsigned i;
-    for (i = 0; i < blines.size(); ++i)
-        morgue_file.add_formatted_string(blines[i], true);
-
-    morgue_file.show();
+    android_show_character_file(morgue_path.c_str());
 }
 
 class UIHiscoresMenu : public Widget
