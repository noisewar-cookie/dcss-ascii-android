<?xml version="1.0" encoding="utf-8"?>
<!-- Root EditText, no wrapping ScrollView: a multiline EditText scrolls
     itself and keeps its cursor visible; nesting it in a fill_parent
     ScrollView breaks both. clipToPadding=false lets the IME inset be
     applied as bottom padding (see ConfigEditor) without clipping text. -->
<EditText xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/note"
    android:layout_width="fill_parent"
    android:layout_height="fill_parent"
    android:inputType="textMultiLine|textNoSuggestions"
    android:background="@android:color/transparent"
    android:textSize="12sp"
    android:padding="5dip"
    android:clipToPadding="false"
    android:gravity="top"
    android:scrollbars="vertical"
/>
