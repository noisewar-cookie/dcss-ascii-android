<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/scroll_view"
    android:layout_width="fill_parent"
    android:layout_height="fill_parent"
    android:scrollbars="vertical">
<!-- wrap_content TextView inside a HorizontalScrollView: the text measures
     to its longest line, so nothing wraps (ascii tables stay aligned) and
     overflow is reached by horizontal drag. Text size comes from
     font_config.txt (char_file_viewer_font_size), set in CharFileViewer. -->
<HorizontalScrollView
    android:id="@+id/hscroll_view"
    android:layout_width="fill_parent"
    android:layout_height="wrap_content"
    android:scrollbars="horizontal">
<TextView
    android:id="@+id/note"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:background="@android:color/transparent"
    android:padding="5dip"
    android:gravity="top"
    android:typeface="monospace"
/>
</HorizontalScrollView>
</ScrollView>
